# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
misc/
# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov
.nyc_output/

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# Vuepress build output
.vuepress/dist

# Vuepress v2.x temp and cache directory
.temp
.cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Build output
dist/
build/
lib/
out/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace
*.sublime-project

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Test coverage
coverage/
.nyc_output/
*.lcov

# Temporary files
tmp/
temp/
.tmp/

# Database files
*.db
*.sqlite
*.sqlite3

# Lock files (uncomment if you want to ignore them)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# TypeScript
*.tsbuildinfo

# ESLint
.eslintcache

# Prettier
.prettierignore

# Stylelint
.stylelintcache

# Jest
.jest/

# Webpack
.webpack/

# Rollup
.rollup.cache/

# Parcel
.parcel-cache/

# Vite
.vite/

# SvelteKit
.svelte-kit/

# Turborepo
.turbo/

# Vercel
.vercel/

# Netlify
.netlify/

# AWS
.aws/

# Docker
.dockerignore

# Kubernetes
*.kubeconfig

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Ansible
*.retry

# Vagrant
.vagrant/

# Local development
.local/
.cache/

# Backup files
*.bak
*.backup
*.old
*.orig

# Archive files
*.zip
*.tar.gz
*.rar
*.7z

# Certificate files
*.pem
*.key
*.crt
*.csr

# API keys and secrets (be extra careful with these)
secrets/
.secrets/
api-keys/
.api-keys/

# Vault storage (contains encrypted wallet keys)
.vault/

# Application specific
uploads/
storage/
public/uploads/
public/storage/

# Monitoring and analytics
.sentry/
.datadog/

# Documentation build
docs/build/
docs/dist/

# Storybook
storybook-static/

# Chromatic
chromatic-build/

# Playwright
test-results/
playwright-report/
playwright/.cache/

# Cypress
cypress/videos/
cypress/screenshots/
cypress/downloads/

# E2E test artifacts
e2e-results/
e2e-reports/

# Performance test results
performance-results/

# Load test results
load-test-results/

# Benchmark results
benchmark-results/

# Profiling results
profiling-results/

# Memory dumps
*.heapsnapshot
*.cpuprofile

# PM2
.pm2/

# Forever
.forever/

# StrongLoop
.strong-pm/

# Nodemon
.nodemonignore

# Grunt
.grunt/

# Gulp
.gulp/

# Webpack Bundle Analyzer
webpack-bundle-analyzer-report.html

# Bundle analysis
bundle-analysis/

# Size limit
.size-limit.json

# Lighthouse CI
.lighthouseci/

# Web Test Runner
.web-test-runner-cache/

# Wireit
.wireit/

# Rush
common/temp/
common/deploy/
common/config/rush/pnpm-lock.yaml

# Nx
.nx/cache
.nx/workspace-data

# Lerna
lerna-debug.log*

# Changesets
.changeset/

# Release Please
release-please-config.json
.release-please-manifest.json

# Husky
.husky/_/

# Commitizen
.cz-config.js

# Semantic Release
.semantic-release/

# Auto-generated files
CHANGELOG.md
CHANGELOG.txt

# Temporary documentation and task files
TASK-*.md
BUGFIX.md
FIXES-APPLIED.md
DEBUG-*.md
FIX-*.md
SETUP*.md
QUICK*.md
POSTMAN*.md
TESTING*.md
VERIFICATION*.md
WALLET-*.md
CIP-30-*.md
CARDANO-*.md
CHALLENGE-*.md
CORRECTED_*.md
FINAL_*.md
GET_*.md
INSTALL-*.md
MAILGUN-*.md
SESSION-*.md
SIGNATURE-*.md
SUPABASE_*.md
ARCHITECTURE.md
backend.md
GETTING-STARTED.md
README_DOCUMENT_UPLOAD.md

# Keep important documentation
!README.md

# GraphQL
schema.graphql
schema.json

# OpenAPI/Swagger
swagger.json
swagger.yaml
api-docs.json

# Postman collections and test files
*.postman_collection.json
*.postman_environment.json
postman-*.json
postman/
postman-setup-guide.md

# Test scripts and temporary files
test-*.http
test-*.ps1
test-*.js
test-*.ts
test-*.md
*.http
run-tests.js
verify-implementation.ps1
verify-implementation.ts

# Migration and setup scripts (temporary)
apply-*.js
apply-*.ps1
apply-*.sql
fix-*.sql
run-migration*.js
run-migration*.ts
run-migration*.cjs
setup-*.js

# Examples and demos
examples/
scripts/
misc/

# PowerShell scripts (temporary)
*.ps1

# SQL files (temporary migrations)
*.sql

# Temporary JavaScript files
apply-email-verification-migration.js
setup-ethereal.js

# Insomnia
.insomnia/

# Thunder Client
thunder-tests/

# REST Client
*.http
*.rest

# Database migrations (if auto-generated)
# migrations/

# Prisma
# prisma/migrations/

# Sequelize
# seeders/
# migrations/

# TypeORM
# migration/

# Knex
# knexfile.js

# Redis dump
dump.rdb

# MongoDB
*.mongodb

# Elasticsearch
elasticsearch/

# Solr
solr/

# Lucene
lucene/

# Search indexes
indexes/

# Cache directories
.cache/
.parcel-cache/
.rollup.cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/
.rpt2_cache/

# Serverless Framework
.serverless/
serverless.yml.bak

# CDK
cdk.out/
cdk.context.json

# Pulumi
Pulumi.*.yaml
.pulumi/

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Supabase
.supabase/

# PlanetScale
.pscale/

# Railway
.railway/

# Render
.render/

# Heroku
.heroku/

# DigitalOcean
.do/

# Google Cloud
.gcloud/
.google/

# Azure
.azure/

# IBM Cloud
.bluemix/

# Oracle Cloud
.oci/

# Alibaba Cloud
.aliyun/

# Tencent Cloud
.qcloud/

# Baidu Cloud
.bce/

# Local development overrides
docker-compose.override.yml
.env.override

# JetBrains IDEs
.idea/
*.iml
*.ipr
*.iws

# Eclipse
.metadata
.recommenders/
.project
.classpath
.settings/

# NetBeans
nbproject/
nbbuild/
nbdist/
.nb-gradle/

# Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# Rider
.idea/
*.sln.iml

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*

# Vim
*.swp
*.swo
*~
.netrwhist

# Sublime Text
*.sublime-project
*.sublime-workspace

# Atom
.atom/

# Brackets
.brackets.json

# Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Local History for Visual Studio Code
.history/

# Built Visual Studio Code Extensions
*.vsix

# FTP deployment
.ftpconfig
.ftppass
.ftpsync.settings

# SFTP deployment
sftp-config.json
.sftpconfig

# Rsync deployment
.rsync_cache/

# Deployment scripts
deploy.sh
deploy.bat
deploy.ps1

# CI/CD
.github/workflows/secrets/
.gitlab-ci-local/
.circleci/config.local.yml

# Monitoring
.newrelic/
.datadog/
.honeycomb/

# Error tracking
.bugsnag/
.rollbar/

# Analytics
.mixpanel/
.amplitude/
.segment/

# A/B testing
.optimizely/
.launchdarkly/

# Feature flags
.flagsmith/
.split/

# Internationalization
*.pot
*.po~

# Localization
locale/
locales/
i18n/

# Translation files
translations/
lang/
languages/

# Font files (if not needed in repo)
*.woff
*.woff2
*.ttf
*.otf
*.eot

# Image optimization cache
.imageoptim-cache/

# Video files (usually too large for git)
*.mp4
*.avi
*.mov
*.wmv
*.flv
*.webm

# Audio files (usually too large for git)
*.mp3
*.wav
*.flac
*.aac
*.ogg

# Large data files
*.csv
*.json.gz
*.xml.gz
*.sql.gz

# Machine learning models
*.model
*.pkl
*.h5
*.pb

# Jupyter notebooks checkpoints
.ipynb_checkpoints/

# Python (if mixed project)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/

# Ruby (if mixed project)
*.gem
*.rbc
/.config
/coverage/
/InstalledFiles
/pkg/
/spec/reports/
/spec/examples.txt
/test/tmp/
/test/version_tmp/
/tmp/
.bundle/
vendor/bundle
lib/bundler/man/

# PHP (if mixed project)
/vendor/
composer.phar
composer.lock
.env.php

# Java (if mixed project)
*.class
*.jar
*.war
*.ear
hs_err_pid*

# Go (if mixed project)
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
vendor/

# Rust (if mixed project)
target/
Cargo.lock

# Swift (if mixed project)
.build/
Packages/
*.xcodeproj
*.xcworkspace

# Kotlin (if mixed project)
*.kt~

# Scala (if mixed project)
*.class
*.log
.cache
.history
.lib/
dist/*
target/
lib_managed/
src_managed/
project/boot/
project/plugins/project/

# Clojure (if mixed project)
pom.xml
pom.xml.asc
*.jar
*.class
/lib/
/classes/
/target/
/checkouts/
.lein-deps-sum
.lein-repl-history
.lein-plugins/
.lein-failures
.nrepl-port

# Haskell (if mixed project)
dist
dist-*
cabal-dev
*.o
*.hi
*.chi
*.chs.h
*.dyn_o
*.dyn_hi
.hpc
.hsenv
.cabal-sandbox/
cabal.sandbox.config
*.prof
*.aux
*.hp
*.eventlog
.stack-work/
cabal.project.local
cabal.project.local~
.HTF/
.ghc.environment.*

# Elixir (if mixed project)
/_build
/cover
/deps
/doc
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/

# Erlang (if mixed project)
.concrete/logs
.rebar
.rebar3
_*
.eunit
deps
*.o
*.beam
*.plt
*.crashdump
erl_crash.dump

# C/C++ (if mixed project)
*.o
*.a
*.so
*.dll
*.exe
*.out
*.app

# Objective-C/Objective-C++ (if mixed project)
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Unity (if mixed project)
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
Assets/AssetStoreTools*
/[Ll]ogs/
/[Mm]emoryCaptures/
/[Uu]serSettings/
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Unreal Engine (if mixed project)
Binaries/
DerivedDataCache/
Intermediate/
Saved/
.vscode/
.vs/
*.VC.db
*.opensdf
*.opendb
*.sdf
*.sln
*.suo
*.xcodeproj
*.xcworkspace

# Godot (if mixed project)
.import/
export.cfg
export_presets.cfg
.mono/
data_*/

# Blender (if mixed project)
*.blend1
*.blend2

# Adobe (if mixed project)
*.psd
*.ai
*.eps

# Sketch (if mixed project)
*.sketch

# Figma (if mixed project)
*.fig

# InVision (if mixed project)
*.invision

# Zeplin (if mixed project)
*.zeplin

# Marvel (if mixed project)
*.marvel

# Principle (if mixed project)
*.prd

# Framer (if mixed project)
*.framer

# ProtoPie (if mixed project)
*.pie

# Origami (if mixed project)
*.origami

# Flinto (if mixed project)
*.flinto

# Kite (if mixed project)
*.kite

# Atomic (if mixed project)
*.atomic

# UXPin (if mixed project)
*.uxpin

# Balsamiq (if mixed project)
*.bmpr

# Axure (if mixed project)
*.rp

# Mockplus (if mixed project)
*.mp

# Justinmind (if mixed project)
*.vp

# Pencil (if mixed project)
*.ep

# Cacoo (if mixed project)
*.cacoo

# Lucidchart (if mixed project)
*.lucidchart

# Draw.io (if mixed project)
*.drawio

# Miro (if mixed project)
*.miro

# Mural (if mixed project)
*.mural

# Conceptboard (if mixed project)
*.conceptboard

# RealtimeBoard (if mixed project)
*.rtb

# Stormboard (if mixed project)
*.storm

# Padlet (if mixed project)
*.padlet

# Trello (if mixed project)
*.trello

# Asana (if mixed project)
*.asana

# Monday (if mixed project)
*.monday

# Notion (if mixed project)
*.notion

# Airtable (if mixed project)
*.airtable

# Coda (if mixed project)
*.coda

# ClickUp (if mixed project)
*.clickup

# Linear (if mixed project)
*.linear

# Height (if mixed project)
*.height

# Shortcut (if mixed project)
*.shortcut

# Clubhouse (if mixed project)
*.clubhouse

# Pivotal Tracker (if mixed project)
*.pivotal

# Jira (if mixed project)
*.jira

# Azure DevOps (if mixed project)
*.azure

# GitHub (if mixed project)
*.github

# GitLab (if mixed project)
*.gitlab

# Bitbucket (if mixed project)
*.bitbucket

# SourceForge (if mixed project)
*.sourceforge

# CodeCommit (if mixed project)
*.codecommit

# Gitea (if mixed project)
*.gitea

# Gitiles (if mixed project)
*.gitiles

# Gerrit (if mixed project)
*.gerrit

# Phabricator (if mixed project)
*.phabricator

# Fossil (if mixed project)
*.fossil

# Bazaar (if mixed project)
*.bzr

# Mercurial (if mixed project)
*.hg

# Subversion (if mixed project)
*.svn

# Perforce (if mixed project)
*.p4

# Team Foundation Server (if mixed project)
*.tfs

# Visual SourceSafe (if mixed project)
*.vss

# Vault (if mixed project)
*.vault

# Surround SCM (if mixed project)
*.surround

# AccuRev (if mixed project)
*.accurev

# StarTeam (if mixed project)
*.starteam

# Synergy (if mixed project)
*.synergy

# Dimensions (if mixed project)
*.dimensions

# PVCS (if mixed project)
*.pvcs

# MKS (if mixed project)
*.mks

# Rational ClearCase (if mixed project)
*.clearcase

# IBM Configuration Management (if mixed project)
*.ibm

# CA Harvest (if mixed project)
*.harvest

# Borland StarTeam (if mixed project)
*.borland

# Microsoft Visual SourceSafe (if mixed project)
*.microsoft

# Telelogic Synergy (if mixed project)
*.telelogic

# PTC Integrity (if mixed project)
*.ptc

# Siemens TeamCenter (if mixed project)
*.siemens

# Dassault ENOVIA (if mixed project)
*.dassault

# Autodesk Vault (if mixed project)
*.autodesk

# SolidWorks PDM (if mixed project)
*.solidworks

# Inventor Vault (if mixed project)
*.inventor

# Creo Windchill (if mixed project)
*.creo

# CATIA ENOVIA (if mixed project)
*.catia

# NX TeamCenter (if mixed project)
*.nx

# Solid Edge Insight (if mixed project)
*.solidedge

# KeyCreator (if mixed project)
*.keycreator

# SpaceClaim (if mixed project)
*.spaceclaim

# Rhino (if mixed project)
*.rhino

# SketchUp (if mixed project)
*.sketchup

# Fusion 360 (if mixed project)
*.fusion360

# Onshape (if mixed project)
*.onshape

# Tinkercad (if mixed project)
*.tinkercad

# 123D (if mixed project)
*.123d

# Maya (if mixed project)
*.maya

# 3ds Max (if mixed project)
*.3dsmax

# Cinema 4D (if mixed project)
*.cinema4d

# Houdini (if mixed project)
*.houdini

# Modo (if mixed project)
*.modo

# ZBrush (if mixed project)
*.zbrush

# Mudbox (if mixed project)
*.mudbox

# Substance (if mixed project)
*.substance

# Mari (if mixed project)
*.mari

# Nuke (if mixed project)
*.nuke

# After Effects (if mixed project)
*.aep

# Premiere Pro (if mixed project)
*.prproj

# Final Cut Pro (if mixed project)
*.fcpx

# Avid (if mixed project)
*.avid

# DaVinci Resolve (if mixed project)
*.resolve

# Lightworks (if mixed project)
*.lightworks

# HitFilm (if mixed project)
*.hitfilm

# Vegas Pro (if mixed project)
*.vegas

# Camtasia (if mixed project)
*.camtasia

# Captivate (if mixed project)
*.captivate

# Articulate (if mixed project)
*.articulate

# Lectora (if mixed project)
*.lectora

# iSpring (if mixed project)
*.ispring

# Adobe Animate (if mixed project)
*.fla

# Adobe Flash (if mixed project)
*.swf

# Adobe Director (if mixed project)
*.dir

# Adobe Authorware (if mixed project)
*.a7r

# Adobe Dreamweaver (if mixed project)
*.dwt

# Adobe Muse (if mixed project)
*.muse

# Adobe Edge (if mixed project)
*.edge

# Adobe Reflow (if mixed project)
*.reflow

# Adobe Comp (if mixed project)
*.comp

# Adobe Preview (if mixed project)
*.preview

# Adobe Proto (if mixed project)
*.proto

# Adobe Ideas (if mixed project)
*.ideas

# Adobe Debut (if mixed project)
*.debut

# Adobe Collage (if mixed project)
*.collage

# Adobe Kuler (if mixed project)
*.kuler

# Adobe Color (if mixed project)
*.color

# Adobe Capture (if mixed project)
*.capture

# Adobe Mix (if mixed project)
*.mix

# Adobe Fix (if mixed project)
*.fix

# Adobe Lightroom (if mixed project)
*.lightroom

# Adobe Bridge (if mixed project)
*.bridge

# Adobe Camera Raw (if mixed project)
*.cameraraw

# Adobe Speedgrade (if mixed project)
*.speedgrade

# Adobe Prelude (if mixed project)
*.prelude

# Adobe Audition (if mixed project)
*.audition

# Adobe Soundbooth (if mixed project)
*.soundbooth

# Adobe Encore (if mixed project)
*.encore

# Adobe OnLocation (if mixed project)
*.onlocation

# Adobe Ultra (if mixed project)
*.ultra

# Adobe Contribute (if mixed project)
*.contribute

# Adobe GoLive (if mixed project)
*.golive

# Adobe ImageReady (if mixed project)
*.imageready

# Adobe LiveMotion (if mixed project)
*.livemotion

# Adobe Dimensions (if mixed project)
*.dimensions

# Adobe PageMaker (if mixed project)
*.pagemaker

# Adobe FrameMaker (if mixed project)
*.framemaker

# Adobe RoboHelp (if mixed project)
*.robohelp

# Adobe Captivate (if mixed project)
*.captivate

# Adobe Presenter (if mixed project)
*.presenter

# Adobe Connect (if mixed project)
*.connect

# Adobe Acrobat (if mixed project)
*.acrobat

# Adobe Reader (if mixed project)
*.reader

# Adobe Digital Editions (if mixed project)
*.digitaleditions

# Adobe Flash Player (if mixed project)
*.flashplayer

# Adobe AIR (if mixed project)
*.air

# Adobe Shockwave (if mixed project)
*.shockwave

# Adobe ColdFusion (if mixed project)
*.coldfusion

# Adobe LiveCycle (if mixed project)
*.livecycle

# Adobe Flex (if mixed project)
*.flex

# Adobe BlazeDS (if mixed project)
*.blazeds

# Adobe LCDS (if mixed project)
*.lcds

# Adobe Scene7 (if mixed project)
*.scene7

# Adobe Omniture (if mixed project)
*.omniture

# Adobe Test&Target (if mixed project)
*.testandtarget

# Adobe SearchCenter (if mixed project)
*.searchcenter

# Adobe Recommendations (if mixed project)
*.recommendations

# Adobe Social (if mixed project)
*.social

# Adobe Media Optimizer (if mixed project)
*.mediaoptimizer

# Adobe Audience Manager (if mixed project)
*.audiencemanager

# Adobe Campaign (if mixed project)
*.campaign

# Adobe Experience Manager (if mixed project)
*.experiencemanager

# Adobe Analytics (if mixed project)
*.analytics

# Adobe Target (if mixed project)
*.target

# Adobe Primetime (if mixed project)
*.primetime

# Adobe Marketing Cloud (if mixed project)
*.marketingcloud

# Adobe Creative Cloud (if mixed project)
*.creativecloud

# Adobe Document Cloud (if mixed project)
*.documentcloud

# Adobe Experience Cloud (if mixed project)
*.experiencecloud

# Microsoft Office (if mixed project)
*.doc
*.docx
*.xls
*.xlsx
*.ppt
*.pptx
*.mdb
*.accdb
*.pub
*.vsd
*.vsdx
*.one
*.onetoc2
*.potx
*.ppsx
*.ppsm
*.potm
*.ppam
*.pptm
*.sldx
*.sldm

# LibreOffice (if mixed project)
*.odt
*.ods
*.odp
*.odg
*.odf

# Google Docs (if mixed project)
*.gdoc
*.gsheet
*.gslides
*.gdraw
*.gform
*.gsite
*.gjam

# Apple iWork (if mixed project)
*.pages
*.numbers
*.key

# Corel (if mixed project)
*.cdr
*.cmx
*.cpt

# QuarkXPress (if mixed project)
*.qxd
*.qxt

# InDesign (if mixed project)
*.indd
*.idml
*.indt
*.indb
*.indl
*.inx

# Scribus (if mixed project)
*.sla
*.slaz

# TeX/LaTeX (if mixed project)
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.idx
*.ilg
*.ind
*.lof
*.log
*.lot
*.out
*.run.xml
*.synctex.gz
*.toc
*.xdv

# BibTeX (if mixed project)
*.bib

# Markdown (if mixed project)
*.md~

# reStructuredText (if mixed project)
*.rst~

# AsciiDoc (if mixed project)
*.adoc~

# Org-mode (if mixed project)
*.org~

# Textile (if mixed project)
*.textile~

# MediaWiki (if mixed project)
*.wiki~

# Confluence (if mixed project)
*.confluence~

# Notion (if mixed project)
*.notion~

# Obsidian (if mixed project)
*.obsidian~

# Roam (if mixed project)
*.roam~

# Logseq (if mixed project)
*.logseq~

# Dendron (if mixed project)
*.dendron~

# Foam (if mixed project)
*.foam~

# Zettlr (if mixed project)
*.zettlr~

# Typora (if mixed project)
*.typora~

# Mark Text (if mixed project)
*.marktext~

# Zettlr (if mixed project)
*.zettlr~

# Notable (if mixed project)
*.notable~

# Boostnote (if mixed project)
*.boostnote~

# Joplin (if mixed project)
*.joplin~

# Standard Notes (if mixed project)
*.standardnotes~

# Bear (if mixed project)
*.bear~

# Ulysses (if mixed project)
*.ulysses~

# Scrivener (if mixed project)
*.scriv~

# WriteRoom (if mixed project)
*.writeroom~

# OmniOutliner (if mixed project)
*.oo3~

# MindMeister (if mixed project)
*.mindmeister~

# XMind (if mixed project)
*.xmind~

# FreeMind (if mixed project)
*.mm~

# Freeplane (if mixed project)
*.freeplane~

# SimpleMind (if mixed project)
*.simplemind~

# MindManager (if mixed project)
*.mindmanager~

# ConceptDraw (if mixed project)
*.conceptdraw~

# Lucidchart (if mixed project)
*.lucidchart~

# Draw.io (if mixed project)
*.drawio~

# Visio (if mixed project)
*.vsd~
*.vsdx~

# OmniGraffle (if mixed project)
*.graffle~

# Gliffy (if mixed project)
*.gliffy~

# Creately (if mixed project)
*.creately~

# SmartDraw (if mixed project)
*.smartdraw~

# EdrawMax (if mixed project)
*.edrawmax~

# yEd (if mixed project)
*.yed~

# Dia (if mixed project)
*.dia~

# Inkscape (if mixed project)
*.svg~

# GIMP (if mixed project)
*.xcf~

# Krita (if mixed project)
*.kra~

# Paint.NET (if mixed project)
*.pdn~

# Affinity (if mixed project)
*.afdesign~
*.afphoto~
*.afpub~

# Canva (if mixed project)
*.canva~

# Figma (if mixed project)
*.figma~

# Sketch (if mixed project)
*.sketch~

# Adobe XD (if mixed project)
*.xd~

# InVision (if mixed project)
*.invision~

# Marvel (if mixed project)
*.marvel~

# Principle (if mixed project)
*.principle~

# Framer (if mixed project)
*.framer~

# ProtoPie (if mixed project)
*.protopie~

# Origami (if mixed project)
*.origami~

# Flinto (if mixed project)
*.flinto~

# Kite (if mixed project)
*.kite~

# Atomic (if mixed project)
*.atomic~

# UXPin (if mixed project)
*.uxpin~

# Balsamiq (if mixed project)
*.balsamiq~

# Axure (if mixed project)
*.axure~

# Mockplus (if mixed project)
*.mockplus~

# Justinmind (if mixed project)
*.justinmind~

# Pencil (if mixed project)
*.pencil~

# Cacoo (if mixed project)
*.cacoo~

# Miro (if mixed project)
*.miro~

# Mural (if mixed project)
*.mural~

# Conceptboard (if mixed project)
*.conceptboard~

# RealtimeBoard (if mixed project)
*.realtimeboard~

# Stormboard (if mixed project)
*.stormboard~

# Padlet (if mixed project)
*.padlet~

# Trello (if mixed project)
*.trello~

# Asana (if mixed project)
*.asana~

# Monday (if mixed project)
*.monday~

# Notion (if mixed project)
*.notion~

# Airtable (if mixed project)
*.airtable~

# Coda (if mixed project)
*.coda~

# ClickUp (if mixed project)
*.clickup~

# Linear (if mixed project)
*.linear~

# Height (if mixed project)
*.height~

# Shortcut (if mixed project)
*.shortcut~

# Clubhouse (if mixed project)
*.clubhouse~

# Pivotal Tracker (if mixed project)
*.pivotal~

# Jira (if mixed project)
*.jira~

# Azure DevOps (if mixed project)
*.azure~

# GitHub (if mixed project)
*.github~

# GitLab (if mixed project)
*.gitlab~

# Bitbucket (if mixed project)
*.bitbucket~

# SourceForge (if mixed project)
*.sourceforge~

# CodeCommit (if mixed project)
*.codecommit~

# Gitea (if mixed project)
*.gitea~

# Gitiles (if mixed project)
*.gitiles~

# Gerrit (if mixed project)
*.gerrit~

# Phabricator (if mixed project)
*.phabricator~

# Fossil (if mixed project)
*.fossil~

# Bazaar (if mixed project)
*.bazaar~

# Mercurial (if mixed project)
*.mercurial~

# Subversion (if mixed project)
*.subversion~

# Perforce (if mixed project)
*.perforce~

# Team Foundation Server (if mixed project)
*.tfs~

# Visual SourceSafe (if mixed project)
*.vss~

# Vault (if mixed project)
*.vault~

# Surround SCM (if mixed project)
*.surround~

# AccuRev (if mixed project)
*.accurev~

# StarTeam (if mixed project)
*.starteam~

# Synergy (if mixed project)
*.synergy~

# Dimensions (if mixed project)
*.dimensions~

# PVCS (if mixed project)
*.pvcs~

# MKS (if mixed project)
*.mks~

# Rational ClearCase (if mixed project)
*.clearcase~

# IBM Configuration Management (if mixed project)
*.ibm~

# CA Harvest (if mixed project)
*.harvest~

# Borland StarTeam (if mixed project)
*.borland~

# Microsoft Visual SourceSafe (if mixed project)
*.microsoft~

# Telelogic Synergy (if mixed project)
*.telelogic~

# PTC Integrity (if mixed project)
*.ptc~

# Siemens TeamCenter (if mixed project)
*.siemens~

# Dassault ENOVIA (if mixed project)
*.dassault~

# Autodesk Vault (if mixed project)
*.autodesk~

# SolidWorks PDM (if mixed project)
*.solidworks~

# Inventor Vault (if mixed project)
*.inventor~

# Creo Windchill (if mixed project)
*.creo~

# CATIA ENOVIA (if mixed project)
*.catia~

# NX TeamCenter (if mixed project)
*.nx~

# Solid Edge Insight (if mixed project)
*.solidedge~

# KeyCreator (if mixed project)
*.keycreator~

# SpaceClaim (if mixed project)
*.spaceclaim~

# Rhino (if mixed project)
*.rhino~

# SketchUp (if mixed project)
*.sketchup~

# Fusion 360 (if mixed project)
*.fusion360~

# Onshape (if mixed project)
*.onshape~

# Tinkercad (if mixed project)
*.tinkercad~

# 123D (if mixed project)
*.123d~

# Maya (if mixed project)
*.maya~

# 3ds Max (if mixed project)
*.3dsmax~

# Cinema 4D (if mixed project)
*.cinema4d~

# Houdini (if mixed project)
*.houdini~

# Modo (if mixed project)
*.modo~

# ZBrush (if mixed project)
*.zbrush~

# Mudbox (if mixed project)
*.mudbox~

# Substance (if mixed project)
*.substance~

# Mari (if mixed project)
*.mari~

# Nuke (if mixed project)
*.nuke~

# After Effects (if mixed project)
*.aftereffects~

# Premiere Pro (if mixed project)
*.premiere~

# Final Cut Pro (if mixed project)
*.finalcut~

# Avid (if mixed project)
*.avid~

# DaVinci Resolve (if mixed project)
*.resolve~

# Lightworks (if mixed project)
*.lightworks~

# HitFilm (if mixed project)
*.hitfilm~

# Vegas Pro (if mixed project)
*.vegas~

# Camtasia (if mixed project)
*.camtasia~

# Captivate (if mixed project)
*.captivate~

# Articulate (if mixed project)
*.articulate~

# Lectora (if mixed project)
*.lectora~

# iSpring (if mixed project)
*.ispring~

